export default {
    setExam(state, exam) {
        state.exam = exam;
    },
    setAnswers(state, answers) {
        state.answers = answers;
    },
    setExamResult(state, result) {
        state.result = result;
    }
};
